#ifndef __RGBSTRIP_TYPES_H__

#define __RGBSTRIP_TYPES_H__

struct rgbstatus
{
    unsigned char r, g, b, l;
};

/* Program states and the current state */
/*
 * Not used yet
 *
enum {
  MODE_OFF,
  MODE_ON,
  MODE_FULL,
  MODE_SLEEP,
  MODE_FLASH,
  MODE_SMOOTH,
  MODE_LOVE
};
*/

#endif
